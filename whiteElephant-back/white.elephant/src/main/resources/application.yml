spring:
  application:
    name: white.elephant

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/whiteelephant?serverTimezone=Asia/Seoul
    username: trio
    password: trio

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        # show_sql: true
        format_sql: true

    database-platform: org.hibernate.dialect.MySQLDialect